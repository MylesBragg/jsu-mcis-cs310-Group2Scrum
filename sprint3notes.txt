acceptance for optional arguments that can float locations
	or optional arguments that can hold multiple values
	
any card that does not have an acc test should probably 
	benefit from acc tests
	in order of priority (top down)
	
	use cases - regression tests to prevent code from getting worse
	
	multiple mains constructed
	
	named required argument (entered in like an optional argument)
	